import "../styles/globals.css";
			export const metadata = {
				title: "Login Pages",
			};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
		}